// src/index.tsx
import React, { useEffect } from "react";
var WIDGET_SCRIPT_PATH = "https://reviews.beaver.codes/widget/web-google-reviews.js";
function GoogleReviewsWidget({ instanceId }) {
  useEffect(() => {
    const divRef = document.querySelector(`div[data-instance-id="${instanceId}"]`);
    if (!divRef) return;
    const script = document.createElement("script");
    script.async = true;
    script.src = WIDGET_SCRIPT_PATH;
    divRef.append(script);
    return () => {
      divRef.removeChild(script);
    };
  }, [instanceId]);
  return /* @__PURE__ */ React.createElement("div", { "data-instance-id": instanceId });
}
var index_default = GoogleReviewsWidget;
export {
  index_default as default
};
